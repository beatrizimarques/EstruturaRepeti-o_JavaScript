let n = parseInt(prompt("Digite o número que deseja ver a tabuada:"))
let multiplo = 1

while ( multiplo <=10){
    document.write( n + "x" + multiplo + "=" + (n*multiplo) + "</br>")
    multiplo++
}
